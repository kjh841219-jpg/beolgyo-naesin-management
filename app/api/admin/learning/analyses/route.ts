import { database,ensureLearningSchema,requireAdmin } from "../_shared";
export async function GET(request:Request){if(!(await requireAdmin()))return Response.json({error:"관리자 로그인이 필요합니다."},{status:401});const id=Number(new URL(request.url).searchParams.get("studentId")),db=database();await ensureLearningSchema(db);const{results}=await db.prepare("SELECT id,exam_name AS examName,exam_date AS examDate,score,total_score AS totalScore,question_errors AS questionErrors,mistake_types AS mistakeTypes,mistake_reasons AS mistakeReasons,difficult_units AS difficultUnits,good_points AS goodPoints,action_plan AS actionPlan FROM exam_analyses WHERE student_id=? ORDER BY exam_date DESC,id DESC").bind(id).all();return Response.json({items:results??[]})}
