"use client";import{FormEvent,useState}from"react";export default function LearningResultUpload(){const[msg,setMsg]=useState(""),[saving,setSaving]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setSaving(true);const f=new FormData(e.currentTarget);f.set("questionType","학습 결과물");f.set("wrongReason",String(f.get("learningNote")||""));f.set("correctReason","학습 결과물 제출");f.set("retryPlan","선생님 피드백 후 보완");const r=await fetch("/api/student/wrong-answers",{method:"POST",body:f}),d=await r.json();setSaving(false);setMsg(r.ok?"사진을 제출하고 영역별 기초 분석 문구를 만들었습니다. 선생님이 사진 확인 후 최종 피드백을 작성합니다.":d.error);if(r.ok)e.currentTarget.reset()}return <section className="wa-shell"><div className="wa-head"><div><span>MY LEARNING OUTPUT</span><h2>학습 결과물 사진 제출</h2><p>필기, 문제풀이, 암기 확인, 서술형 연습 결과를 사진으로 올리세요.</p></div></div><form className="wa-form" onSubmit={submit}><label className="wa-upload"><strong>📷 학습 결과물 사진</strong><span>JPG·PNG·WEBP / 최대 8MB</span><input required name="photo" type="file" accept="image/jpeg,image/png,image/webp" capture="environment"/></label><div className="wa-grid"><label>학습일<input required name="studyDate" type="date" defaultValue={new Date().toISOString().slice(0,10)}/></label><label>결과물 제목<input required name="title" placeholder="예: 3과 본문 암기 필기"/></label><label className="wa-wide">스스로 평가한 내용<textarea name="learningNote" placeholder="잘한 점, 어려웠던 점, 선생님께 확인받고 싶은 내용을 적어 주세요."/></label></div><button disabled={saving}>{saving?"사진 올리는 중…":"학습 결과물 제출"}</button>{msg&&<p className="wa-message">{msg}</p>}</form></section>}
